<view class="share">
  <canvas class="canvas-hide postcanvas" canvas-id="share" style="width:{{canvasWidth*2}}rpx;height:{{canvasHeight*2}}rpx" />
  <view class="content" style="transform:scale({{responsiveScale}});-webkit-transform:scale({{responsiveScale}});">
    <image class="canvas" src="{{imageFile}}" style="width:{{canvasWidth}}rpx;height:{{canvasHeight}}rpx" />
    <view class="footer" style="width:{{canvasWidth}}rpx">
      <view class="save" bindtap="handleSave">保存到相册</view>
      <view class="close" bindtap="handleClose">关闭</view>
    </view>
  </view>
</view>