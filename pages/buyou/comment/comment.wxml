<!--pages/buyou/comment/comment.wxml-->
<view class="container hastop">
<!--  <dk-nav title="添加评论" hasBackBtn="{{true}}" isFixed="{{true}}" bind:backTo="backTo"></dk-nav>
 -->  <view class="textarea">
    <textarea
    placeholder="{{'回复' + targetName}}"
    placeholder-style="color:#999;"
    bindinput="bindinput"
    />
  </view>
  <view class="tr">
    <view class="sendBtn btn-default" bindtap="sendComment">发送</view>
  </view>
</view>

